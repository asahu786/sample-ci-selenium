<build>
  <plugins>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
      <version>3.2.5</version>
      <configuration>
        <!-- Point to your TestNG suite -->
        <suiteXmlFiles>
          <suiteXmlFile>src/test/resources/testng.xml</suiteXmlFile>
        </suiteXmlFiles>

        <!-- Optional: if your test classes don’t match defaults, relax includes -->
        <includes>
          <include>**/*.java</include>
        </includes>

        <!-- Ensure TestNG is the provider -->
        <properties>
          <property>
            <name>listener</name>
            <value>org.testng.reporters.XMLReporter</value>
          </property>
        </properties>

        <!-- Make failures fail the build -->
        <testFailureIgnore>false</testFailureIgnore>
      </configuration>
      <dependencies>
        <dependency>
          <groupId>org.testng</groupId>
          <artifactId>testng</artifactId>
          <version>7.10.2</version>
        </dependency>
      </dependencies>
    </plugin>
  </plugins>
</build>
